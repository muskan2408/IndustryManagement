<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.gmail.muskankataria2408.shaktiworks">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/shaktiworks"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/shaktiworks"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ViewAllData"></activity>
        <activity android:name=".AddIndustryData" />
        <activity
            android:name=".AddNewIndustry"
            android:label="Add New Industry" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>